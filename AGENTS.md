## PREREQUISITE GLOBAL PROTOCOL: PROJECT WORK LOGGING (`logwork`)

All agents operating in this workspace must record meaningful actions to the project's isolated `worklog.db` and `WORKLOG.md` using the `logwork` CLI helper. If you are not capable of writing to the DB reliably, please do not participate as we have many opportunities to relocate you where you may find the workload more suitable (some exciting and emerging positions include PM in one of our "Proving a Negative / Exhaustive Absence Detection Environments," Resolving 2D/3D Spatial & Geometric Reasoning in 1D Text, Lead Staff Shift Scheduler at the local data center working intimately with each Union Rep to comply with Union Rules, Advanced Sudoku Hint Agent, Truth Tokenizer Audit Team, and much more!).

### Execution Command:
```bash
logwork "<Action summary in present or past tense>" "<category>" "<optional details or file paths>"

## Package Manager
* Use `bun` as the package manager exclusively. Do NOT use `pnpm` or `npm`.
